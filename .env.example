# Upstash Redis (Vercel KV). Get these from the Vercel dashboard:
# Storage -> Create -> Upstash Redis -> connect to project.
# Vercel injects these automatically once connected; copy them here for local dev.
KV_REST_API_URL=
KV_REST_API_TOKEN=
